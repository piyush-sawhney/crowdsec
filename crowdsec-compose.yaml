services:
  crowdsec:
    image: crowdsecurity/crowdsec:latest
    container_name: crowdsec
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      COLLECTIONS: >-
        crowdsecurity/linux
        crowdsecurity/traefik
        crowdsecurity/sshd
        crowdsecurity/appsec-virtual-patching
        crowdsecurity/appsec-generic-rules
        crowdsecurity/base-http-scenarios
        crowdsecurity/http-cve
        crowdsecurity/whitelist-good-actors
      # Keep ONLY Traefik since we are removing the host-firewall
      BOUNCER_KEY_traefik: "${CROWDSEC_TRAEFIK_KEY}"
      TZ: Asia/Kolkata
      # LISTEN_ADDR 0.0.0.0 is required for Docker bridge networking
      LAPI_LISTEN_ADDR: 0.0.0.0:8080
      # Use the container's internal address for the local agent
      LOCAL_API_URL: http://127.0.0.1:8080
    volumes:
      - ./configs/acquis/appsec.yaml:/etc/crowdsec/acquis.d/appsec.yaml:ro
      - ./configs/acquis/traefik.yaml:/etc/crowdsec/acquis.d/traefik.yaml:ro
      - ./configs/acquis/ssh.yaml:/etc/crowdsec/acquis.d/ssh.yaml:ro
      - ./configs/profiles.yaml:/etc/crowdsec/profiles.yaml:ro
      - ./configs/notifications/telegram.yaml:/etc/crowdsec/notifications/http.yaml:ro
      - crowdsec-db:/var/lib/crowdsec/data/
      - crowdsec-config:/etc/crowdsec/
      - traefik-logs:/var/log/traefik:ro
      - /var/log/auth.log:/var/log/auth.log:ro
    ports:
      # Removed 127.0.0.1 binding for a moment to verify connectivity
      - "8081:8080"
    networks:
      - traefik-public
    mem_limit: 512m
    cpus: "0.5"
    healthcheck:
      test: ["CMD", "cscli", "version"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

volumes:
  crowdsec-db:
  crowdsec-config:
  traefik-logs:
    external: true

networks:
  traefik-public:
    external: true